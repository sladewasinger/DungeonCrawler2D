/** Presentation-only tuning for the outdoor entity camera cull. */
export const ENTITY_PRESENTATION_TUNING = {
  /** Keeps sprites, nameplates, and fast projectiles from popping at the viewport edge. */
  cullMarginTiles: 3,
} as const;
